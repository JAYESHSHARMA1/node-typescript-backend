import * as responseMessage from "./response-message";

// user roles
export const USER_ROLES: {
  OWNER: string;
  USER: string;
} = {
  OWNER: "owner",
  USER: "user",
};

// response messages
export const RESPONSES = responseMessage;
