import Notification from "./../models/Notification";

export const notifyFollowers = (
  userId: string,
  notification: any,
  notificationType: string
) => {};

export const notifyUser = (
  userId: string,
  notification: any,
  notificationType: string
) => {};
